apiVersion: v1
kind: Service
metadata:
  name: {{ include "finances.frontend.fullname" . }}
  labels:
    {{- include "finances.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finances.frontend.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "finances.backend.fullname" . }}
  labels:
    {{- include "finances.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finances.backend.selectorLabels" . | nindent 4 }}

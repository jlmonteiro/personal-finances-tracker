apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "finances.backend.fullname" . }}-config
  labels:
    {{- include "finances.backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
